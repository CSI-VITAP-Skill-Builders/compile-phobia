services:
  - type: web
    name: compile-phobia-backend
    runtime: node
    startCommand: cd backend && npm install && npm start
    buildCommand: cd backend && npm install
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
      - key: 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
# Optional override
        fromGroup: compile-phobia-env

  - type: web
    name: compile-phobia-frontend
    runtime: node
    startCommand: cd devsync-frontend && npm install && npm run build && npx serve -s dist -l 3000
    buildCommand: cd devsync-frontend && npm install && npm run build
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
      - key: VITE_API_BASE_URL
        fromGroup: compile-phobia-env

envVarGroups:
  - name: compile-phobia-env
    envVars:
      - key: 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
# Optional override
        scope: all
